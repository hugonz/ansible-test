- hosts: localhost
  gather_fact: false
  tasks:
    - name: Print all the structure from all hosts that are in the inventory
      ansible.builtin.debug:
        msg: "A host: {{ hostvars[item] }}"
      loop: "{{ groups.all }}"
