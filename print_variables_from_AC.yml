- hosts: localhost
  gather_facts: false
  environment: "{{ hostvars[inventory_host] }}"
  tasks:
    - name: Get environment
      ansible.builtin.command:
        cmd: printenv
      register: _
      
    - name: Print environment
      ansible.builtin.debug:
        var: "{{ _.stdout }}"

      
